name: new_flutter_firebase_login
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.18.2 <3.0.0'


dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter
  intl:

  cupertino_icons: ^1.0.2

  firebase_core: 
  firebase_auth: 
  google_sign_in: 
  shared_preferences: ^2.0.18
  get: ^4.6.5
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  
  # flutter_lints: ^2.0.0


flutter:
  generate: true
  uses-material-design: true
  
  assets:
    - assets/images/


